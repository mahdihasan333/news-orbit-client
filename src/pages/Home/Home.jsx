import { Helmet } from "react-helmet-async";

const Home = () => {
    return (
        <div>
            <Helmet>
                <title>NewsOrbit || Home</title>
            </Helmet>
            <h2>home</h2>
        </div>
    );
};

export default Home;